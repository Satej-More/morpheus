/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove typedRoutes - not widely supported and can cause build failures
  // on some Next 14 minor versions
};

module.exports = nextConfig;
